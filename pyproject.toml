[project]
name = "delay-detection-pipeline"
version = "0.1.0"
description = "Delay detection pipeline"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "faststream[cli,kafka]>=0.6.7",
    "polars>=1.38.1",
]
